[package]
name = "rendero-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core document model for Rendero — nodes, tree, properties, hit testing, layout"

[dependencies]
glam = { workspace = true }
serde = { workspace = true }
